# copy to .env and fill in. .env is gitignored.
OPENAI_API_KEY=sk-...
# AGENT_MODEL=gpt-5.5
# AGENT_PROXY=http://127.0.0.1:7890   # set to "none" to connect directly
# MOONSHOT_API_KEY=sk-...             # use Kimi instead of OpenAI
# SHOW_SLICE=1                        # print the slice each turn
