name = "hello"
version = "0.1.0"
description = "Reference plugin: contributes a tool and a skill via register(ctx)."
# Drop a plugin directory like this into .sliceagent/plugins/ (project) or ~/.sliceagent/plugins/
# (user), or point [plugins].dirs in sliceagent.toml at a parent directory of it.
